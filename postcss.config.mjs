// What is diff betw postcss vs tialwind config?
  // PostCSS is a tool for transforming CSS with JavaScript plugins, while Tailwind CSS is a utility-first CSS framework that can be used as a PostCSS plugin to generate utility classes based on your configuration. 
const config = {
  plugins: ["@tailwindcss/postcss"],
  
};

export default config;
